{
    "name": "villa-site",
    "version": "1.0.0",
    "scripts": {
        "dev": "vite",
        "build": "vite build",
        "serve": "vite preview",
        "deploy": "npm run build && gh-pages -d dist"
    },
    "dependencies": {
        "react": "^18.x",
        "react-dom": "^18.x",
        "react-router-dom": "^6.x",
        "emailjs-com": "^3.x"
    },
    "devDependencies": {
        "@vitejs/plugin-react": "^4.x",
        "gh-pages": "^5.x",
        "vite": "^4.x"
    }
}